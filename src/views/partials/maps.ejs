<div id="googleMap" style="width:100%;height:100%;"></div>

<script>
  function myMap() {
    var mapProp = {
      center: new google.maps.LatLng(19, 72),
      zoom: 15
    };
    var map = new google.maps.Map(
      document.getElementById("googleMap"),
      mapProp
    );

    getLocation();

    function getLocation() {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(showPosition);
      } else {
        alert(
          "Geolocation is not supported by this browser please enter your location"
        );
      }
    }

    function showPosition(position) {
      addMarker(
        {
          lat: position.coords.latitude,
          lng: position.coords.longitude
        },
        "https://cdn2.iconfinder.com/data/icons/basic-thin-line-color/21/09-512.png"
      );
    }
    function addMarker(coords, ico) {
      var icon = {
        url: ico, // url
        scaledSize: new google.maps.Size(50, 50), // scaled size
        origin: new google.maps.Point(0, 0), // origin
        anchor: new google.maps.Point(0, 0) // anchor
      };
      var marker = new google.maps.Marker({ position: coords, icon: icon });
      marker.setMap(map);
      map.setCenter(coords);
      marker.addListener("click", function() {
        infoWindow.open(map, marker);
      });
    }

    var infoWindow = new google.maps.InfoWindow({
      content: "<p>You're Here</p>"
    });

    showHospitals();

    function showHospitals() {
      var geocodingapi = "AIzaSyBtmeBm2D7DrewbQVzgFQ7ORAbVKLPoF8U"
      var hospitals = [{ lat: 19.0761854, lng: 72.8864673 }];

      for (var i = 0; i < hospitals.length; i++) {
        addMarker(hospitals[i]);
      }
    }
  }
</script>

<script
  async
  defer
  src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD4t5rzWW8jbSp5RG8nwKLwW7GICssed9o&callback=myMap"
></script>
